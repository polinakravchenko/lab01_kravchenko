const N = 7; 
const pi = 3.14;

alert ("Варіант: " + N + "\nКравченко Поліна");

function FunctionNumFirst() {
    let rad = 2 * N;
    let heigh = N + 4;
    let V = pi * rad * rad * heigh;
    alert ("Об'єм: " + V);
}